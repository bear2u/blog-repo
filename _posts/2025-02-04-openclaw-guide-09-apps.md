---
layout: post
title: "OpenClaw 완벽 가이드 (9) - 앱 & 노드"
date: 2025-02-04
permalink: /openclaw-guide-09-apps/
author: Peter Steinberger
categories: [AI 에이전트, OpenClaw]
tags: [OpenClaw, macOS, iOS, Android, Apps, Nodes]
original_url: "https://github.com/openclaw/openclaw"
excerpt: "OpenClaw의 macOS, iOS, Android 앱과 노드 시스템을 알아봅니다."
---

## 앱 개요

OpenClaw는 **선택적 컴패니언 앱**을 제공합니다. Gateway만으로도 완전한 경험을 제공하지만, 앱은 추가 기능을 제공합니다.

```
┌─────────────────────────────────────────────────────────────────┐
│                    OpenClaw Apps                                 │
│                                                                  │
│   ┌─────────────┐   ┌─────────────┐   ┌─────────────┐          │
│   │  macOS App  │   │   iOS App   │   │ Android App │          │
│   └──────┬──────┘   └──────┬──────┘   └──────┬──────┘          │
│          │                 │                 │                  │
│          └─────────────────┼─────────────────┘                  │
│                            │                                    │
│                            ▼                                    │
│                     Gateway (WS)                                │
│                   ws://127.0.0.1:18789                          │
│                            │                                    │
│                            ▼                                    │
│                    Node Capabilities                            │
│          (camera, screen, location, system)                     │
└─────────────────────────────────────────────────────────────────┘
```

---

## macOS 앱

### 주요 기능

| 기능 | 설명 |
|------|------|
| **메뉴 바 컨트롤** | Gateway 상태, 빠른 액세스 |
| **Voice Wake** | 웨이크 워드로 음성 활성화 |
| **Talk Mode** | 연속 음성 대화 |
| **WebChat** | 내장 웹 채팅 |
| **노드 모드** | 디바이스 기능 노출 |
| **원격 Gateway** | SSH로 원격 Gateway 제어 |

### 설치

```bash
# Homebrew (권장)
brew install --cask openclaw

# 또는 직접 다운로드
# https://github.com/openclaw/openclaw/releases
```

### 메뉴 바 UI

```
┌─────────────────────────────────────────────────────────────────┐
│                    macOS Menu Bar                                │
│                                                                  │
│   🦞 ▼                                                          │
│   ├── Status: Connected                                         │
│   ├── ────────────────                                          │
│   ├── Voice Wake [✓]                                            │
│   ├── Talk Mode  [ ]                                            │
│   ├── ────────────────                                          │
│   ├── Open WebChat                                              │
│   ├── Open Canvas                                               │
│   ├── ────────────────                                          │
│   ├── Settings...                                               │
│   ├── Debug Tools...                                            │
│   └── Quit                                                      │
└─────────────────────────────────────────────────────────────────┘
```

### 노드 모드

macOS 앱은 **노드 모드**로 Gateway에 연결됩니다:

```typescript
// macOS 노드가 제공하는 명령
{
  commands: [
    "system.run",      // 시스템 명령 실행
    "system.notify",   // 알림 전송
    "canvas.push",     // Canvas 업데이트
    "canvas.eval",     // Canvas JS 실행
    "canvas.snapshot", // Canvas 스크린샷
    "camera.snap",     // 카메라 스냅샷
    "camera.clip",     // 카메라 클립 녹화
    "screen.record",   // 화면 녹화
    "location.get",    // 위치 정보
  ]
}
```

### 권한 요구사항

| 권한 | 용도 |
|------|------|
| **마이크** | Voice Wake, Talk Mode |
| **카메라** | camera.snap, camera.clip |
| **화면 녹화** | screen.record, system.run (일부) |
| **알림** | system.notify |
| **위치 서비스** | location.get |
| **접근성** | 키보드 단축키 (선택) |

### 빌드 (개발)

```bash
# Xcode 프로젝트 열기
cd apps/macos
xcodegen generate
open OpenClaw.xcodeproj

# 빌드
xcodebuild -project OpenClaw.xcodeproj \
  -scheme OpenClaw \
  -configuration Debug \
  build
```

---

## iOS 앱

### 주요 기능

| 기능 | 설명 |
|------|------|
| **Canvas** | 에이전트 UI 표시 |
| **Voice Wake** | 웨이크 워드 지원 |
| **Talk Mode** | 음성 대화 |
| **카메라** | 스냅샷, 클립 |
| **화면 녹화** | 화면 캡처 |
| **Bonjour 페어링** | 자동 Gateway 발견 |

### 페어링

```
┌─────────────────────────────────────────────────────────────────┐
│                    iOS Pairing Flow                              │
│                                                                  │
│   1. iOS 앱 실행                                                │
│   2. 같은 네트워크의 Gateway 자동 발견 (Bonjour)                │
│   3. 페어링 코드 입력 또는 QR 스캔                              │
│   4. 연결 완료                                                   │
│                                                                  │
│   수동 연결:                                                    │
│   Gateway URL: ws://192.168.1.100:18789                         │
│   Token: <gateway-token>                                        │
└─────────────────────────────────────────────────────────────────┘
```

### 노드 명령

```bash
# iOS 노드 명령 실행
openclaw nodes invoke iphone camera.snap

# 화면 녹화
openclaw nodes invoke iphone screen.record --duration 10

# 위치 정보
openclaw nodes invoke iphone location.get
```

### 빌드 (개발)

```bash
# Xcode 프로젝트 생성
cd apps/ios
xcodegen generate
open OpenClaw.xcodeproj

# 시뮬레이터에서 실행
xcodebuild -project OpenClaw.xcodeproj \
  -scheme OpenClaw \
  -destination "platform=iOS Simulator,name=iPhone 17" \
  -configuration Debug \
  build

# 시뮬레이터 실행
xcrun simctl boot "iPhone 17"
xcrun simctl launch booted ai.openclaw.ios
```

---

## Android 앱

### 주요 기능

| 기능 | 설명 |
|------|------|
| **Canvas** | 에이전트 UI 표시 |
| **Talk Mode** | 음성 대화 |
| **카메라** | 스냅샷, 클립 |
| **화면 녹화** | 화면 캡처 |
| **SMS** | SMS 전송 (선택) |

### 페어링

iOS와 동일한 Bridge + 페어링 흐름을 사용합니다:

```bash
# Android 노드 목록
openclaw nodes list

# 페어링 승인
openclaw pairing approve android <code>
```

### 노드 명령

```bash
# 카메라 스냅샷
openclaw nodes invoke android camera.snap

# 화면 녹화
openclaw nodes invoke android screen.record --duration 5

# 위치 정보
openclaw nodes invoke android location.get
```

### Talk Mode 출력 포맷

Android는 저지연 AudioTrack 스트리밍을 지원합니다:

| 포맷 | 설명 |
|------|------|
| `pcm_16000` | 16kHz PCM |
| `pcm_22050` | 22.05kHz PCM |
| `pcm_24000` | 24kHz PCM (기본) |
| `pcm_44100` | 44.1kHz PCM |

### 빌드 (개발)

```bash
# 빌드
cd apps/android
./gradlew :app:assembleDebug

# 설치 및 실행
./gradlew :app:installDebug
adb shell am start -n ai.openclaw.android/.MainActivity

# 테스트
./gradlew :app:testDebugUnitTest
```

---

## 원격 Gateway

Gateway를 원격 서버에서 실행하고, 앱/CLI에서 연결할 수 있습니다.

### 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                    Remote Gateway                                │
│                                                                  │
│   ┌─────────────┐         ┌─────────────┐                       │
│   │  Remote     │   SSH   │  Local      │                       │
│   │  Gateway    │◀───────▶│  macOS App  │                       │
│   │  (Linux)    │  Tunnel │  CLI        │                       │
│   └─────────────┘         └─────────────┘                       │
│         │                                                       │
│         ▼                                                       │
│   WhatsApp/Telegram/etc.                                        │
│   (채널 연결은 Gateway에서)                                      │
│                                                                  │
│   ┌─────────────┐                                               │
│   │  Local      │                                               │
│   │  Nodes      │──────────▶ (디바이스 액션만 로컬)             │
│   │  (macOS)    │                                               │
│   └─────────────┘                                               │
└─────────────────────────────────────────────────────────────────┘
```

### Tailscale 설정

```json5
{
  gateway: {
    tailscale: {
      mode: "serve",  // tailnet 내부 접근
      // mode: "funnel",  // 공개 접근 (password 필수)
    },
    bind: "loopback",
    auth: {
      mode: "password",  // funnel 시 필수
      password: "secure-password",
    },
  },
}
```

### SSH 터널

```bash
# 로컬에서 원격 Gateway 연결
ssh -N -L 18789:127.0.0.1:18789 user@remote-server

# macOS 앱 설정에서 "Remote Gateway" 활성화
# URL: ws://127.0.0.1:18789
```

### 원격 vs 로컬 실행

| 기능 | 실행 위치 |
|------|-----------|
| **bash 도구** | Gateway 호스트 |
| **파일 읽기/쓰기** | Gateway 호스트 |
| **채널 연결** | Gateway 호스트 |
| **system.run** | 노드 (로컬 디바이스) |
| **camera/screen** | 노드 (로컬 디바이스) |
| **알림** | 노드 (로컬 디바이스) |

---

## 노드 관리

### 노드 목록

```bash
# 연결된 노드 확인
openclaw nodes list

# 출력 예:
# ID              Type     Status    Caps
# macbook-pro     macos    online    camera,screen,system,canvas,location
# iphone-15       ios      online    camera,screen,canvas,location
# pixel-8         android  offline   camera,screen,canvas,location
```

### 노드 상세

```bash
# 노드 기능 확인
openclaw nodes describe macbook-pro

# 출력:
# Node: macbook-pro
# Type: macos
# Status: online
# Caps: camera, screen, system, canvas, location
# Commands:
#   - system.run
#   - system.notify
#   - camera.snap
#   - camera.clip
#   - screen.record
#   - location.get
#   - canvas.push
#   - canvas.eval
#   - canvas.snapshot
# Permissions:
#   - Microphone: granted
#   - Camera: granted
#   - Screen Recording: granted
#   - Location: granted
#   - Notifications: granted
```

### 노드 명령 실행

```bash
# 시스템 명령
openclaw nodes invoke macbook-pro system.run --command "uptime"

# 알림
openclaw nodes invoke macbook-pro system.notify \
  --title "작업 완료" \
  --body "배포가 성공적으로 완료되었습니다."

# 카메라
openclaw nodes invoke iphone camera.snap --output ~/photo.jpg

# 화면 녹화
openclaw nodes invoke android screen.record \
  --duration 10 \
  --output ~/recording.mp4
```

---

## 다중 노드 시나리오

### 홈 오피스 설정

```
┌─────────────────────────────────────────────────────────────────┐
│                    Multi-Node Setup                              │
│                                                                  │
│   Remote Server (Always-on)                                     │
│   ├── Gateway                                                   │
│   ├── WhatsApp/Telegram 연결                                    │
│   └── Cron 작업 실행                                            │
│                                                                  │
│   MacBook (Work)                                                │
│   ├── Node: macbook-work                                        │
│   ├── system.run (개발 환경)                                    │
│   └── camera/screen                                             │
│                                                                  │
│   iPhone (Mobile)                                               │
│   ├── Node: iphone                                              │
│   ├── 이동 중 음성 대화                                         │
│   └── camera/location                                           │
└─────────────────────────────────────────────────────────────────┘
```

### 노드 선택 로직

에이전트는 작업에 적합한 노드를 자동 선택합니다:

```typescript
// 에이전트가 사진을 찍어야 할 때
// 1. 사용 가능한 노드 중 camera capability 가진 노드 탐색
// 2. 온라인 노드 우선
// 3. 사용자가 명시적으로 노드 지정 가능

"iphone에서 사진 찍어줘"  // → iphone 노드 사용
"맥북으로 화면 녹화해줘"   // → macbook 노드 사용
```

---

*다음 글에서는 보안과 배포를 살펴봅니다.*
