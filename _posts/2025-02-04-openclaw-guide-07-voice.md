---
layout: post
title: "OpenClaw 완벽 가이드 (7) - 음성 & Talk Mode"
date: 2025-02-04
permalink: /openclaw-guide-07-voice/
author: Peter Steinberger
categories: [AI]
tags: [OpenClaw, Voice, Talk Mode, ElevenLabs, Speech, TTS]
original_url: "https://github.com/openclaw/openclaw"
excerpt: "OpenClaw의 Voice Wake, Talk Mode, ElevenLabs TTS 통합을 알아봅니다."
---

## 음성 기능 개요

OpenClaw는 **항상 켜진 음성 대화**를 지원합니다:

```
┌─────────────────────────────────────────────────────────────────┐
│                    Voice Architecture                            │
│                                                                  │
│   ┌───────────────┐                                             │
│   │  Voice Wake   │ ──▶ "Hey Molty" ──▶ 활성화                  │
│   └───────────────┘                                             │
│           │                                                     │
│           ▼                                                     │
│   ┌───────────────┐                                             │
│   │  Talk Mode    │ ──▶ 연속 음성 대화 루프                     │
│   └───────────────┘                                             │
│           │                                                     │
│           ▼                                                     │
│   ┌───────────────┐                                             │
│   │  ElevenLabs   │ ──▶ 고품질 TTS 음성 출력                    │
│   └───────────────┘                                             │
└─────────────────────────────────────────────────────────────────┘
```

---

## Voice Wake

**Voice Wake**는 웨이크 워드로 에이전트를 깨우는 기능입니다.

### 작동 방식

1. 항상 마이크 청취 (저전력)
2. 웨이크 워드 감지 ("Hey Molty", "Okay Molty")
3. 음성 캡처 시작
4. 침묵 감지 시 전사 및 전송

### 설정

```json5
// ~/.openclaw/openclaw.json
{
  voicewake: {
    enabled: true,
    keyword: "molty",  // "alexa" | "jarvis" | "hey google" 등
    sensitivity: 0.5,  // 0.0 ~ 1.0

    // 전사 설정
    transcription: {
      provider: "whisper",  // "whisper" | "openai-whisper-api"
    },

    // 액션
    action: {
      type: "agent",  // 에이전트에게 전달
      sessionKey: "main",
    },
  },
}
```

### macOS 앱에서 사용

```
┌─────────────────────────────────────────────────────────────────┐
│                    macOS Voice Wake                              │
│                                                                  │
│   1. 메뉴 바 → OpenClaw 아이콘                                  │
│   2. "Voice Wake" 토글 활성화                                   │
│   3. 마이크 권한 허용                                           │
│   4. "Hey Molty!" 또는 설정된 웨이크 워드 말하기                │
│                                                                  │
│   상태 표시:                                                    │
│   🟢 대기 중 (웨이크 워드 청취)                                 │
│   🟡 녹음 중 (음성 캡처)                                        │
│   🔵 처리 중 (전사/응답)                                        │
└─────────────────────────────────────────────────────────────────┘
```

---

## Talk Mode

**Talk Mode**는 연속 음성 대화 루프입니다:

```
┌─────────────────────────────────────────────────────────────────┐
│                    Talk Mode Loop                                │
│                                                                  │
│   1. 🎤 Listening ──▶ 음성 청취                                 │
│          │                                                      │
│          ▼ (침묵 감지)                                          │
│   2. 🧠 Thinking ──▶ 전사 → 모델 전송                          │
│          │                                                      │
│          ▼ (응답 생성)                                          │
│   3. 🔊 Speaking ──▶ ElevenLabs TTS 재생                       │
│          │                                                      │
│          └──────────▶ 1번으로 돌아감                            │
│                                                                  │
│   인터럽트: 사용자가 말하면 재생 중단                           │
└─────────────────────────────────────────────────────────────────┘
```

### 설정

```json5
{
  talk: {
    // ElevenLabs 설정
    voiceId: "elevenlabs_voice_id",
    modelId: "eleven_v3",
    apiKey: "elevenlabs_api_key",  // 또는 ELEVENLABS_API_KEY 환경 변수

    // 출력 포맷
    outputFormat: "mp3_44100_128",  // macOS/iOS: pcm_44100, Android: pcm_24000

    // 인터럽트 설정
    interruptOnSpeech: true,  // 사용자가 말하면 중단

    // 음성 파라미터
    stability: 0.5,       // 0.0, 0.5, 1.0 (eleven_v3)
    similarity: 0.75,     // 유사도 부스트
    style: 0.0,           // 스타일 강도
    speakerBoost: true,   // 스피커 부스트
  },
}
```

### 환경 변수

```bash
export ELEVENLABS_API_KEY="your-api-key"
export ELEVENLABS_VOICE_ID="your-voice-id"
```

---

## ElevenLabs 통합

### 음성 선택

ElevenLabs는 다양한 음성을 제공합니다:

| 카테고리 | 음성 예시 |
|----------|-----------|
| **Premade** | Rachel, Adam, Antoni, Josh |
| **Cloned** | 커스텀 음성 클론 |
| **Community** | 커뮤니티 공유 음성 |

### 음성 지시어

에이전트 응답에서 음성을 동적으로 변경할 수 있습니다:

```json
{ "voice": "new-voice-id", "once": true }
```

**예시:**

```
에이전트 응답:
{ "voice": "pirate-voice-id", "once": true }
안녕하세요, 해적 목소리로 말씀드리겠습니다!
```

### 지원 파라미터

```json
{
  "voice": "voice-id",       // 음성 ID
  "voiceId": "voice-id",     // 별칭
  "model": "eleven_v3",      // 모델 ID
  "speed": 1.0,              // 속도 (WPM)
  "stability": 0.5,          // 안정성
  "similarity": 0.75,        // 유사도
  "style": 0.0,              // 스타일
  "speakerBoost": true,      // 부스트
  "once": true               // 이번 응답만 적용
}
```

---

## macOS Talk Mode UI

### 오버레이 상태

```
┌─────────────────────────────────────────────────────────────────┐
│                    Talk Mode Overlay                             │
│                                                                  │
│   🎤 Listening                                                  │
│   • 구름 아이콘이 마이크 레벨에 따라 펄스                       │
│   • 파란색 테두리                                                │
│                                                                  │
│   🧠 Thinking                                                   │
│   • 구름 아이콘이 가라앉는 애니메이션                           │
│   • 노란색 테두리                                                │
│                                                                  │
│   🔊 Speaking                                                   │
│   • 구름 아이콘에서 파동이 퍼지는 애니메이션                    │
│   • 녹색 테두리                                                  │
│   • 클릭하면 재생 중단                                          │
│                                                                  │
│   ❌ 종료                                                        │
│   • X 버튼 클릭으로 Talk Mode 종료                              │
└─────────────────────────────────────────────────────────────────┘
```

### 메뉴 바 컨트롤

- **Talk** 토글: Talk Mode 활성화/비활성화
- **설정 탭**: 음성 ID, 인터럽트 토글

---

## iOS/Android Talk Mode

### iOS

```
┌─────────────────────────────────────────────────────────────────┐
│                    iOS Talk Mode                                 │
│                                                                  │
│   1. OpenClaw iOS 앱 설치                                       │
│   2. Gateway에 페어링                                           │
│   3. Talk 버튼 탭                                               │
│   4. 음성으로 대화                                               │
│                                                                  │
│   Features:                                                     │
│   • 저지연 PCM 스트리밍                                         │
│   • 백그라운드 오디오                                            │
│   • Siri 통합 (선택)                                            │
└─────────────────────────────────────────────────────────────────┘
```

### Android

```
┌─────────────────────────────────────────────────────────────────┐
│                    Android Talk Mode                             │
│                                                                  │
│   지원 출력 포맷:                                               │
│   • pcm_16000                                                   │
│   • pcm_22050                                                   │
│   • pcm_24000 (기본)                                            │
│   • pcm_44100                                                   │
│                                                                  │
│   AudioTrack 스트리밍으로 저지연 재생                           │
└─────────────────────────────────────────────────────────────────┘
```

---

## Whisper 전사

음성을 텍스트로 변환하는 데 **Whisper**를 사용합니다:

### 로컬 Whisper

```bash
# whisper 설치
pip install openai-whisper

# 또는 whisper.cpp
brew install whisper-cpp
```

```json5
{
  voicewake: {
    transcription: {
      provider: "whisper",
      model: "base",  // tiny, base, small, medium, large
    },
  },
}
```

### OpenAI Whisper API

```json5
{
  voicewake: {
    transcription: {
      provider: "openai-whisper-api",
      // OPENAI_API_KEY 환경 변수 필요
    },
  },
}
```

---

## 음성 관련 스킬

### openai-whisper

```bash
# 오디오 파일 전사
whisper audio.mp3 --model base --language ko
```

### openai-whisper-api

```bash
# API로 전사
curl https://api.openai.com/v1/audio/transcriptions \
  -H "Authorization: Bearer $OPENAI_API_KEY" \
  -F file="@audio.mp3" \
  -F model="whisper-1"
```

### sherpa-onnx-tts

로컬 TTS 대안:

```json5
{
  talk: {
    provider: "sherpa-onnx",
    model: "vits-ko",  // 한국어 모델
  },
}
```

---

## 권한 요구사항

### macOS

| 권한 | 용도 |
|------|------|
| **마이크** | Voice Wake, Talk Mode |
| **Speech Recognition** | 음성 인식 |
| **Accessibility** | 키보드 단축키 (선택) |

### iOS/Android

| 권한 | 용도 |
|------|------|
| **마이크** | 음성 입력 |
| **음성 인식** | 전사 |
| **백그라운드 오디오** | 백그라운드 재생 |

---

## 문제 해결

### 일반적인 문제

| 문제 | 해결책 |
|------|--------|
| "마이크 권한 없음" | 시스템 설정에서 권한 허용 |
| "ElevenLabs 오류" | API 키 확인, 잔액 확인 |
| "음성이 안 들림" | 출력 장치 확인, 볼륨 확인 |
| "웨이크 워드 인식 안됨" | sensitivity 조정, 마이크 확인 |

### 디버그 모드

```bash
# Talk Mode 로그
OPENCLAW_DEBUG_TALK=1 openclaw gateway --verbose
```

---

## 설정 예시: 완전한 음성 설정

```json5
{
  voicewake: {
    enabled: true,
    keyword: "molty",
    sensitivity: 0.5,
    transcription: {
      provider: "openai-whisper-api",
    },
  },

  talk: {
    voiceId: "21m00Tcm4TlvDq8ikWAM",  // Rachel
    modelId: "eleven_v3",
    outputFormat: "pcm_44100",
    interruptOnSpeech: true,
    stability: 0.5,
    similarity: 0.75,
  },
}
```

---

*다음 글에서는 훅과 자동화를 살펴봅니다.*
