---
layout: post
title: "Maestro 가이드 - Maestro Studio"
date: 2025-02-04
categories: [개발 도구, Maestro]
tags: [maestro, studio, ide, visual-testing, recording]
author: mobile-dev-inc
original_url: https://github.com/mobile-dev-inc/Maestro
---

## Maestro Studio 소개

**Maestro Studio Desktop**은 테스트를 시각적으로 설계하고 실행할 수 있는 경량 IDE입니다. 터미널 없이 테스트를 작성할 수 있습니다.

```
┌─────────────────────────────────────────────────────────────┐
│                   Maestro Studio                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│   │  Device     │  │   Element   │  │    Flow     │       │
│   │  Preview    │  │  Inspector  │  │   Editor    │       │
│   └─────────────┘  └─────────────┘  └─────────────┘       │
│                                                             │
│   Features:                                                 │
│   • 비주얼 플로우 빌더                                      │
│   • 실시간 디바이스 미러링                                  │
│   • 요소 인스펙터                                           │
│   • 레코딩 모드                                             │
│   • AI 어시스턴트 (MaestroGPT)                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 설치

### 다운로드

[Maestro Studio 다운로드](https://maestro.dev/#maestro-studio)에서 OS에 맞는 버전 다운로드:

- **macOS** (Intel / Apple Silicon)
- **Windows**
- **Linux**

### CLI에서 실행 (브라우저 버전)

```bash
# CLI에서 Studio 실행
maestro studio

# 특정 포트로 실행
maestro studio --port 9999
```

브라우저에서 `http://localhost:9999` 열림

## 인터페이스 구성

### 메인 화면

```
┌─────────────────────────────────────────────────────────────┐
│  [Device] [Inspector] [Flows] [Console]        [Settings]   │
├──────────────────────┬──────────────────────────────────────┤
│                      │                                      │
│   📱 Device          │   📝 Flow Editor                     │
│   Preview            │                                      │
│                      │   - launchApp                        │
│   ┌──────────────┐   │   - tapOn: "Login"                  │
│   │              │   │   - inputText: "user@test.com"      │
│   │   App        │   │   - tapOn: "Submit"                 │
│   │   Screen     │   │                                      │
│   │              │   │   [+ Add Step]                       │
│   └──────────────┘   │                                      │
│                      ├──────────────────────────────────────┤
│   [Tap] [Type]       │   🔍 Element Details                 │
│   [Swipe] [Assert]   │   ID: login_button                   │
│                      │   Text: "Login"                      │
│                      │   Bounds: (100, 200, 300, 250)       │
└──────────────────────┴──────────────────────────────────────┘
```

## 요소 인스펙터

### 요소 선택

1. Device Preview에서 요소 클릭
2. 요소 정보 자동 표시:
   - Resource ID
   - Text
   - Content Description
   - Bounds (좌표)
   - Class Name

### 선택자 복사

```yaml
# 인스펙터에서 자동 생성된 선택자
- tapOn:
    id: "com.example.app:id/login_button"

# 또는
- tapOn: "Login"
```

### 계층 구조 보기

```
📱 Root
├── FrameLayout
│   ├── LinearLayout
│   │   ├── TextView ("Welcome")
│   │   └── Button ("Login")  ← 선택됨
│   └── ImageView
└── NavigationBar
```

## 비주얼 플로우 빌더

### 드래그 앤 드롭으로 명령 추가

1. 왼쪽 패널에서 명령 선택
2. 플로우 에디터로 드래그
3. 파라미터 설정

### 명령 팔레트

| 카테고리 | 명령 |
|----------|------|
| **App** | launchApp, stopApp, clearState |
| **Tap** | tapOn, doubleTapOn, longPressOn |
| **Input** | inputText, pressKey |
| **Navigate** | back, scroll, swipe |
| **Assert** | assertVisible, assertNotVisible |
| **AI** | assertWithAI, extractTextWithAI |

### 명령 편집

```yaml
# 더블클릭으로 편집 모드 진입
- tapOn:
    text: "Login"
    index: 0
    retryTapIfNoChange: true
```

## 레코딩 모드

### 레코딩 시작

1. **Record** 버튼 클릭
2. 디바이스에서 액션 수행
3. 자동으로 YAML 명령 생성

### 레코딩 예시

```
사용자 액션              →    생성된 명령
─────────────────────────────────────────
버튼 탭                  →    - tapOn: "Login"
텍스트 입력              →    - inputText: "user@test.com"
스와이프                 →    - swipe:
                                  direction: "up"
뒤로가기                 →    - back
```

### 레코딩 팁

- **스마트 선택자**: 가장 안정적인 선택자 자동 선택
- **중복 제거**: 연속된 동일 액션 최적화
- **검토 필수**: 생성된 명령 검토 및 수정

## 실시간 테스트 실행

### 단일 명령 실행

```bash
# 커서 위치의 명령만 실행
Cmd/Ctrl + Enter
```

### 플로우 전체 실행

```bash
# 전체 플로우 실행
Cmd/Ctrl + Shift + Enter
```

### 실행 결과 확인

```
✅ launchApp                    0.5s
✅ tapOn: "Login"               0.3s
❌ assertVisible: "Dashboard"   Failed
   └── Element not found after 5000ms
```

## AI 어시스턴트 (MaestroGPT)

### 자연어로 명령 생성

```
입력: "로그인 버튼을 탭하고 이메일 입력 후 제출해줘"

생성:
- tapOn: "Login"
- tapOn:
    id: "email_input"
- inputText: "user@example.com"
- tapOn: "Submit"
```

### 플로우 자동 생성

```
입력: "회원가입 플로우 테스트 만들어줘"

생성:
appId: com.example.app
name: Sign Up Flow
---
- launchApp:
    clearState: true
- tapOn: "Sign Up"
- tapOn: "Email"
- inputRandomEmail
- tapOn: "Password"
- inputText: "TestPassword123!"
- tapOn: "Confirm Password"
- inputText: "TestPassword123!"
- tapOn: "Create Account"
- assertVisible: "Welcome"
```

### 에러 해결 도움

```
에러: Element "Login" not found

AI 제안:
1. 요소 텍스트가 다를 수 있습니다: "Sign In", "Log In"
2. 요소가 아직 로딩 중일 수 있습니다
3. ID로 선택해 보세요: - tapOn: { id: "login_button" }
```

## 프로젝트 관리

### 플로우 파일 구조

```
maestro-studio/
├── flows/
│   ├── login.yaml
│   ├── signup.yaml
│   └── checkout.yaml
├── subflows/
│   ├── common_login.yaml
│   └── common_logout.yaml
└── config.yaml
```

### 환경 설정

```yaml
# config.yaml
defaultAppId: com.example.app
env:
  USERNAME: test@example.com
  PASSWORD: secret123
defaultTimeout: 10000
```

## 디버깅 기능

### 브레이크포인트

```yaml
- launchApp
- tapOn: "Login"
# 🔴 브레이크포인트 - 여기서 일시정지
- inputText: "user@test.com"
```

### 변수 감시

```
Variables:
  USERNAME: "test@example.com"
  aiOutput: "Order #12345"
  index: 2
```

### 스크린샷 히스토리

```
Step 1: launchApp           [📷]
Step 2: tapOn "Login"       [📷]
Step 3: inputText           [📷] ← 현재
```

## 단축키

| 단축키 | 기능 |
|--------|------|
| `Cmd/Ctrl + Enter` | 현재 명령 실행 |
| `Cmd/Ctrl + Shift + Enter` | 전체 플로우 실행 |
| `Cmd/Ctrl + S` | 플로우 저장 |
| `Cmd/Ctrl + /` | 명령 주석 처리 |
| `Cmd/Ctrl + D` | 명령 복제 |
| `F5` | 디바이스 새로고침 |

## 내보내기

### YAML 파일로 저장

```bash
# Studio에서 작성한 플로우를 파일로 저장
File > Export > YAML
```

### CLI에서 실행

```bash
# Studio에서 만든 플로우를 CLI로 실행
maestro test flows/login.yaml
```

## 다음 단계

다음 챕터에서는 Maestro Cloud를 다룹니다.

---

**이전 글**: [AI 통합](/maestro-guide-07-ai/)

**다음 글**: [Maestro Cloud](/maestro-guide-09-cloud/)
