---
layout: post
title: "Context Engineering 완벽 가이드 (2) - 핵심 개념"
date: 2026-02-05
permalink: /context-engineering-guide-02-core-concepts/
author: davidkimai
categories: [AI 에이전트, Context Engineering]
tags: [Context Engineering, Atoms, Molecules, Cells, Organs, Few-Shot]
original_url: "https://github.com/davidkimai/Context-Engineering"
excerpt: "Context Engineering의 4가지 기본 빌딩 블록을 알아봅니다."
---

## 생물학적 메타포 개요

Context Engineering은 생물학에서 영감을 받은 계층적 구조를 사용합니다.

```
┌─────────────────────────────────────────────────────────────┐
│                    생물학적 메타포                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│         Neural Fields                                        │
│              ▲                                               │
│              │                                               │
│         ┌────┴────┐                                         │
│         │         │                                         │
│    ┌────┴───┐ ┌───┴────┐                                   │
│    │        │ │        │                                   │
│  ┌─┴──┐ ┌──┴─┴─┐ ┌──┴─┐ ┌──────┐                         │
│  │Atoms│ │Mole- │ │Cells│ │Organs│                         │
│  └─────┘ │cules │ └─────┘ └──────┘                         │
│          └──────┘                                           │
│   Basic   Few-shot  Stateful  Multi-step                   │
│  Prompting Learning  Memory   Control                       │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## Level 1: Atoms (기본 프롬프팅)

**Atoms**는 가장 기본적인 컨텍스트 단위입니다 - 단일 지시문과 제약조건.

### 특징

- 단일 프롬프트 지시문
- 기본적인 제약조건
- 직접적인 명령

### 예시

```
# 나쁜 예시 (모호함)
"코드 작성해줘"

# 좋은 예시 (명확함)
"Python으로 피보나치 수열을 계산하는 함수를 작성해주세요.
- 입력: 정수 n
- 출력: n번째 피보나치 수
- 재귀 대신 반복문 사용
- 타입 힌트 포함"
```

### Atomic Constraints

```yaml
# 기본 제약조건 템플릿
constraints:
  language: "Korean"
  format: "markdown"
  length: "concise"
  style: "professional"
  output: "code with comments"
```

### 프롬프트 구조

```
┌─────────────────────────────────────────────────────────────┐
│                    Atomic Prompt 구조                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────┐                                            │
│  │   ROLE      │  "You are a senior Python developer..."    │
│  └─────────────┘                                            │
│         │                                                    │
│         ▼                                                    │
│  ┌─────────────┐                                            │
│  │   TASK      │  "Write a function that..."                │
│  └─────────────┘                                            │
│         │                                                    │
│         ▼                                                    │
│  ┌─────────────┐                                            │
│  │ CONSTRAINTS │  "Use type hints, handle errors..."        │
│  └─────────────┘                                            │
│         │                                                    │
│         ▼                                                    │
│  ┌─────────────┐                                            │
│  │   OUTPUT    │  "Return the code in a code block"         │
│  └─────────────┘                                            │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## Level 2: Molecules (Few-Shot Learning)

**Molecules**는 예제를 통한 학습입니다 - 여러 Atoms를 결합하여 패턴을 보여줍니다.

### 특징

- 입출력 예제 쌍
- 패턴 시연
- 암묵적 규칙 전달

### Few-Shot 구조

```
┌─────────────────────────────────────────────────────────────┐
│                    Few-Shot Learning                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Example 1                                            │  │
│  │  Input: "Hello, how are you?"                         │  │
│  │  Output: "안녕하세요, 어떻게 지내세요?"                 │  │
│  └───────────────────────────────────────────────────────┘  │
│                          │                                  │
│                          ▼                                  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Example 2                                            │  │
│  │  Input: "What is your name?"                          │  │
│  │  Output: "당신의 이름은 무엇인가요?"                   │  │
│  └───────────────────────────────────────────────────────┘  │
│                          │                                  │
│                          ▼                                  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Actual Query                                         │  │
│  │  Input: "I love programming"                          │  │
│  │  Output: ?                                            │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 예시: 코드 리뷰

```markdown
## Task: Review code and provide feedback

### Example 1:
**Input:**
```python
def add(a, b):
    return a + b
```

**Output:**
- ✅ 함수명이 명확함
- ⚠️ 타입 힌트 누락
- 💡 제안: `def add(a: int, b: int) -> int:`

### Example 2:
**Input:**
```python
def calc(x):
    return x*2+1
```

**Output:**
- ❌ 함수명이 불명확 (calc → calculate_linear)
- ⚠️ 연산자 주변 공백 부족
- 💡 제안: `return x * 2 + 1`

### Now review this:
**Input:**
```python
def f(n):
    if n<=1: return n
    return f(n-1)+f(n-2)
```
```

### Few-Shot 모범 사례

| 원칙 | 설명 |
|------|------|
| **다양성** | 다양한 케이스를 포함 |
| **일관성** | 동일한 형식 유지 |
| **관련성** | 실제 쿼리와 관련된 예제 |
| **순서** | 쉬운 것에서 어려운 것으로 |
| **개수** | 3-5개가 적절 (너무 많으면 오버헤드) |

---

## Level 3: Cells (상태와 메모리)

**Cells**는 대화 상태와 메모리를 관리합니다 - 정보의 지속성.

### 특징

- 대화 히스토리 유지
- 상태 관리
- 컨텍스트 윈도우 관리

### 메모리 구조

```
┌─────────────────────────────────────────────────────────────┐
│                    Memory Structure                          │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              Working Memory (Short-term)             │    │
│  │  • 현재 대화 컨텍스트                                │    │
│  │  • 최근 도구 호출 결과                               │    │
│  │  • 임시 상태 변수                                    │    │
│  └─────────────────────────────────────────────────────┘    │
│                          │                                  │
│                          ▼                                  │
│  ┌─────────────────────────────────────────────────────┐    │
│  │             Episodic Memory (Events)                 │    │
│  │  • 과거 대화 요약                                    │    │
│  │  • 중요 이벤트 기록                                  │    │
│  │  • 시간순 정보                                       │    │
│  └─────────────────────────────────────────────────────┘    │
│                          │                                  │
│                          ▼                                  │
│  ┌─────────────────────────────────────────────────────┐    │
│  │             Semantic Memory (Knowledge)              │    │
│  │  • 사용자 선호도                                     │    │
│  │  • 도메인 지식                                       │    │
│  │  • 학습된 패턴                                       │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 상태 관리 예시

```python
# 상태 관리 패턴
class ConversationState:
    def __init__(self):
        self.history = []           # 대화 히스토리
        self.user_context = {}      # 사용자 컨텍스트
        self.task_state = {}        # 현재 작업 상태
        self.memory = {
            "short_term": [],       # 최근 N개 메시지
            "long_term": {},        # 영구 저장 정보
            "episodic": []          # 이벤트 기록
        }

    def add_message(self, role: str, content: str):
        self.history.append({
            "role": role,
            "content": content,
            "timestamp": time.time()
        })
        self._update_memory()

    def _update_memory(self):
        # 단기 메모리: 최근 10개만 유지
        self.memory["short_term"] = self.history[-10:]

        # 오래된 메시지는 요약하여 장기 메모리로
        if len(self.history) > 10:
            old_messages = self.history[:-10]
            summary = self._summarize(old_messages)
            self.memory["long_term"]["summary"] = summary
```

### 컨텍스트 윈도우 관리

```
┌───────────────────────────────────────────────────────────┐
│              Context Window Management                     │
├───────────────────────────────────────────────────────────┤
│                                                           │
│  Total Context Window: 128K tokens                        │
│  ┌─────────────────────────────────────────────────────┐ │
│  │████████████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░│ │
│  │◄── System ──►◄── History ──►◄── Available ──────────►│ │
│  │    (10K)         (30K)            (88K)              │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                           │
│  전략:                                                    │
│  • 오래된 메시지 요약                                     │
│  • 중요도 기반 정보 유지                                  │
│  • 동적 압축                                              │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

---

## Level 4: Organs (멀티스텝 제어)

**Organs**는 복잡한 워크플로우와 멀티스텝 제어를 담당합니다.

### 특징

- 멀티스텝 워크플로우
- 제어 흐름
- 오케스트레이션

### 워크플로우 패턴

```
┌─────────────────────────────────────────────────────────────┐
│                    Multi-step Workflow                       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐  │
│  │  Plan   │───►│ Execute │───►│ Verify  │───►│ Refine  │  │
│  └─────────┘    └─────────┘    └─────────┘    └─────────┘  │
│       │              │              │              │        │
│       ▼              ▼              ▼              ▼        │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐  │
│  │ 작업    │    │ 도구    │    │ 결과    │    │ 개선    │  │
│  │ 분해    │    │ 실행    │    │ 검증    │    │ 반복    │  │
│  └─────────┘    └─────────┘    └─────────┘    └─────────┘  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### ReAct 패턴

```
Thought → Action → Observation → Thought → ...

┌─────────────────────────────────────────────────────────────┐
│  Thought: 사용자가 파일 내용을 분석하길 원한다              │
│           먼저 파일을 읽어야 한다                           │
├─────────────────────────────────────────────────────────────┤
│  Action: read_file("data.csv")                              │
├─────────────────────────────────────────────────────────────┤
│  Observation: name,age,city                                 │
│               Alice,30,Seoul                                │
│               Bob,25,Busan                                  │
├─────────────────────────────────────────────────────────────┤
│  Thought: CSV 파일이다. 데이터를 분석하겠다                 │
├─────────────────────────────────────────────────────────────┤
│  Action: analyze_data(...)                                  │
└─────────────────────────────────────────────────────────────┘
```

### 복잡한 오케스트레이션

```python
# Organ-level 워크플로우
class TaskOrchestrator:
    async def execute_complex_task(self, task: str):
        # 1. 작업 분해
        subtasks = await self.decompose(task)

        # 2. 의존성 분석
        execution_order = self.topological_sort(subtasks)

        # 3. 순차/병렬 실행
        results = {}
        for batch in execution_order:
            # 병렬 실행 가능한 작업들
            batch_results = await asyncio.gather(*[
                self.execute_subtask(t) for t in batch
            ])
            results.update(batch_results)

        # 4. 결과 통합
        final_result = await self.synthesize(results)

        # 5. 검증
        if not await self.verify(final_result):
            return await self.refine(final_result)

        return final_result
```

---

## 레벨 비교

| Level | 복잡도 | 상태 | 예시 |
|-------|--------|------|------|
| **Atoms** | 낮음 | 없음 | 단일 질문/답변 |
| **Molecules** | 중간 | 없음 | Few-shot 번역 |
| **Cells** | 중간 | 있음 | 챗봇 대화 |
| **Organs** | 높음 | 있음 | 멀티스텝 코딩 에이전트 |

---

## 다음 단계

다음 글에서는 **Neural Systems**와 **Cognitive Tools**를 다룹니다. 이는 Organs 위에 구축되어 더 정교한 추론과 문제 해결을 가능하게 합니다.

```
   Atoms → Molecules → Cells → Organs → Neural Systems
                                              ↑
                                        다음 글에서
```

---

*다음 글에서는 Neural Systems와 Cognitive Tools를 살펴봅니다.*
